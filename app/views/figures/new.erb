<h1>Create new Figure</h1>
<form action="/figures" method="post">
  Name: <input type="text" id="figure_name" name="figure[name]"><br>
  Title:<br>
  <% @titles.each do |title| %>
      <input type="checkbox" name="figure[title_ids][]" id="title_<%= title.id %>" value="<%= title.id %>"><%= title.name %><br>
  <% end %>
  <br>
  New Title: <input type="text" name="title[name]" id="new_title">
  <br>
  Landmarks:<br>
  <% @landmarks.each do |landmark| %>
      <input type="checkbox" name="figure[landmark_ids][]" id="landmark_<%= landmark.id %>" value="<%= landmark.id %>"><%= landmark.name %><br>
  <% end %>
  </select>
  <br>
  New Landmark: <input type="text" name="landmark[name]" id="new_landmark">
  <br>
  <input type="submit" value="Create New Figure" id="submit">
</form>
